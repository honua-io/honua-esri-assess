"""Bundled JSON Schemas for honua-esri-assess artifacts.

The schemas in this subpackage are the published contract surface read at
runtime via :mod:`importlib.resources`. The canonical, human-readable copy
lives at ``schemas/esri-footprint-v0.1.json`` at the repository root and the
two files are kept in lock-step by ``tests/test_schema_packaging.py``.
"""
